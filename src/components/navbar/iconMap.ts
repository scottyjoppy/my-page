import { Code, HomeIcon, Library, Notebook, User } from "lucide-react";
import { ElementType } from "react";

// Define the type for the IconsMap
export const IconsMap: { [key: string]: ElementType } = {
  HomeIcon,
  User,
  Code,
  Notebook,
  Library,
};
